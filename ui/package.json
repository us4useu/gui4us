{
  "name": "gui4us-ui",
  "version": "0.1.0",
  "private": true,
  "description": "GUI4us view components: ultrasound stream display, control, action and capture panels. The same ES modules are bundled for the web app and loaded by the Jupyter (anywidget) widgets.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "build:widgets": "vite build --config vite.widgets.config.js",
    "build:all": "npm run build && npm run build:widgets"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
